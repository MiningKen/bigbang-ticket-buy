# 寬宏搶票助手：BIGBANG 高雄中信卡友優先購

這是獨立於 Ticket Plus 的寬宏模式，使用 `.kham-profile` 保存寬宏登入狀態，不會讀取或覆寫原本的 Ticket Plus profile。

已套用的購票設定：

- 中國信託全卡友優先購：2026-09-18 14:00（台灣時間）
- 首選：2027-02-28，無可購票券才改嘗試 2027-02-27
- 1 張；不選輪椅／身障席
- 非視線遮蔽區優先，之後按票價高至低嘗試；必要時接受遮蔽區
- 接受 VIP 1 站席，並嘗試加選 VIP 粉絲福利
- 可在控制面板暫存中信卡號前 6 碼；只存在程式記憶體，關閉後自動清除
- 開賣後會重試進入購票頁，從「立即訂購」列表依非遮蔽／票價偏好選擇
- 2/28 找不到可購選項時會在短時間內自動切換 2/27
- 選到票種後最多只會按一次「下一步」，之後停下等待本人確認

## macOS

1. 雙擊 `START-KHAM-MAC.command`。
2. 控制面板開啟後，按「開啟登入頁」，自行登入寬宏並確認實名制名單。
3. 在控制面板輸入中信卡號前 6 碼並按「安全暫存 6 碼」，再按「開始等待 14:00」。
4. 14:00 後，程式偵測到卡友驗證欄位會自動填入前 6 碼並送出；找不到明確的驗證按鈕、遇到排隊或其他驗證時會通知本人接手。
5. 程式選到票種並按一次下一步後，請自行確認實名資料與完成中信卡付款。

macOS 啟動寬宏模式後會同時使用系統 `caffeinate` 防止等待期間自動睡眠；控制面板關閉時會一併停止。

## Windows

完整解壓縮可攜 ZIP 後，雙擊 `START-KHAM-WINDOWS.cmd`。不需要額外安裝 Node.js 或 Playwright；需要 Google Chrome 或 Microsoft Edge。

## 安全界線

程式只會把你在控制面板提供的卡號前 6 碼暫存在記憶體，填入官方卡友驗證欄位，並在辨識到明確的驗證按鈕時自動送出；不會寫入檔案、log 或狀態 API，關閉程式後會自動清除。程式不會儲存或輸入完整卡號、實名資料、驗證碼，也不會送出付款；不會繞過排隊、CAPTCHA 或網站限制。若頁面改版、出現驗證或自動辨識不到票種，控制面板會提示你立即切換到寬宏 Chrome 手動接手。
