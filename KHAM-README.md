# 寬宏搶票助手：BIGBANG 高雄一般販售

這是獨立於 Ticket Plus 的寬宏模式，使用 `.kham-profile` 保存寬宏登入狀態，不會讀取或覆寫原本的 Ticket Plus profile。

## 目前設定

- 一般販售：2026-09-22 10:00（台灣時間），不需要信用卡前 6 碼。
- 日期順位：2/28 優先，其次 2/27。
- 控制台可選「1 張」或「2 張連號優先，失敗則 1 張」。
- 兩張模式的精確順位：2/28 兩張連號 → 2/27 兩張連號 → 2/28 單張 → 2/27 單張。兩張不連號不接受。
- 永遠排除輪椅、身障、無障礙與陪同席；非遮蔽區優先，再依票價由高至低。
- 沒有符合條件的票時，每 3–5 秒使用寬宏頁面的「更新票數」，在兩個日期間循環，直到選到票或你按「停止」。
- 遇到排隊、CAPTCHA、未知訊息、登入問題或 HTTP 403/429 時會停止自動操作並通知你接手。

## macOS

1. 雙擊 `START-KHAM-MAC.command`。
2. 控制台開啟後按「開啟登入頁」，自行登入寬宏，並事先確認實名制名單正確。
3. 在「票數模式」選擇單張或兩張連號優先。
4. 按「開始等待 9/22 10:00」。瀏覽器有開著即可，不需要最大化；請勿關閉購票分頁或讓電腦關機。
5. 程式確認符合條件並按下一步後會發出警示。請立刻切回寬宏，確認實名資料並在期限內完成付款。

macOS 等待期間會使用系統 `caffeinate` 防止自動睡眠；關閉控制台時會一併停止本機服務。

## Windows

完整解壓縮可攜 ZIP 後，雙擊 `START-KHAM-WINDOWS.cmd`。不需要安裝 Node.js、npm 或 Playwright，只需 Google Chrome 或 Microsoft Edge。操作步驟與 macOS 相同。

## 中信卡友演練（非 9/22 正式模式）

只有明確設定 `KHAM_SALE_MODE=ctbc-rehearsal` 時才顯示卡號前 6 碼欄位。前 6 碼只保存在記憶體中，關閉程式後清除；演練抵達票況頁即停止，不會選票或保留座位。

## 安全界線

程式不儲存或輸入完整卡號、實名資料、驗證碼，也不送出付款；不會繞過排隊、CAPTCHA 或網站限制。官方頁面改版或無法確認兩張是否連號時，程式會停止並保留瀏覽器供你檢查。搶票結果仍受官方即時庫存、排隊與付款期限影響，無法保證成功。
